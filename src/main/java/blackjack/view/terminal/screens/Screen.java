package blackjack.view.terminal.screens;

/**
 * Represents a single terminal screen that can render itself.
 */
public interface Screen {
    /**
     * Renders the screen to the terminal.
     */
    void render();
}
